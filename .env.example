# Copy this file to .env on the server or local machine when overrides are needed.
HOST=127.0.0.1
PORT=8000
BASE_PATH=/predict
STATIC_DIR=static
DB_PATH=data/changsha_prediction.db
LOG_DIR=logs
LOG_LEVEL=INFO
